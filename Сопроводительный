📄 Сопроводительные пояснения к проекту

Этот документ объясняет, почему в проекте были приняты определённые архитектурные и технические решения, какие библиотеки выбраны и что важно в реализации.

🧱 Архитектура проекта — почему так?

Проект разбит на несколько отдельных модулей по принципу
“каждый файл отвечает только за одну задачу”.

Так проще поддерживать код, менять логику и добавлять новые функции.

Структура:

main.py — главный файл, который запускает камеру, вызывает детекцию и проверку зон.

detection.py — отвечает только за работу с YOLO и получение людей в кадре.

zone.py — всё, что связано с запретной зоной: загрузка, рисование, проверка на пересечение.

tracker.py (если используется) — отслеживание объектов между кадрами.

zone_config.json — сохранение координат зоны, чтобы можно было редактировать её без изменений в коде.

Почему так?
Чтобы логика не смешивалась в одном месте. Если YOLO обновится — меняем detection.py.
Если зона меняется — редактируем JSON.
Если хотим добавить звуковые уведомления — просто подключаем новый модуль.

🤖 Почему YOLOv8?

YOLOv8 выбран как оптимальное сочетание:

высокой скорости,

лёгкости интеграции,

хорошего качества детекции людей.

Для задач определения людей в запрещённой зоне он подходит идеально:
модель быстро работает даже на обычном ноутбуке и не требует сложной настройки.

Мы используем yolov8n (nano), потому что:

она самая лёгкая,

даёт высокую скорость в реальном времени,

качество распознавания людей — более чем достаточное.

📚 Почему выбраны именно эти библиотеки?
Ultralytics

Новая версия YOLO с удобным API.
Позволяет получить bounding boxes через results[0].boxes.xyxy, что делает код понятным и коротким.

OpenCV

Используется для:

работы с камерой,

рисования зон,

отображения результата.

OpenCV — это стандарт для видеоаналитики, и он идеально подходит для нашей задачи.

JSON

Файл zone_config.json удобен тем, что:

можно хранить координаты зоны отдельно,

можно менять зону без изменения кода,

формат легко читается человеком.

🟦 Как работает запрещённая зона

Когда проект запускается:

Зона загружается из JSON-файла.
Если файла нет — он создаётся.

На экране отображается многоугольник зоны, который пользователь может изменить (если включена функция редактирования).

Каждый кадр проходит обработку:

YOLO находит людей.

Центр каждого человека рассчитывается.

Проверяем, входит ли центр в зону (алгоритм "point in polygon").

Если человек заходит в зону:

подсвечиваем зону красным,

можно дописать: активировать сигнал, уведомление и т.д.

Это простой и надёжный механизм контроля запрещённой зоны.

⚙️ Особенности реализации

Детекция работает кадр за кадром.

Зона хранится отдельно: можно задать любую форму, не только прямоугольник.

Логика проверки точная: используется математический алгоритм определения точки внутри полигона.

Код модульный — его легко расширять.
